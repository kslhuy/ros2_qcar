# Additional requirements for refactored vehicle control system

# Testing
pytest>=7.0.0

# Configuration file support
pyyaml>=6.0

# ZeroMQ for robust YOLO perception streaming (replaces Quanser BasicStream)
pyzmq>=25.0.0

# Note: Core dependencies (numpy, etc.) should already be installed
# from the original QCar setup
