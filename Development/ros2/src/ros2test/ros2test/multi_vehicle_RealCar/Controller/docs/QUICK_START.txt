┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  🎯 QUICK START: Change Controllers in 3 Simple Steps      ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

┌─────────────────────────────────────────────────────────────┐
│ STEP 1: Open the file                                       │
└─────────────────────────────────────────────────────────────┘

File: following_leader_state.py
Path: Development/multi_vehicle_self_driving_RealQcar/qcar/StateMachine/

┌─────────────────────────────────────────────────────────────┐
│ STEP 2: Find line ~48 (look for this code)                 │
└─────────────────────────────────────────────────────────────┘

    def __init__(self, vehicle_logic, config, logger):
        """Initialize with configurable controller type"""
        super().__init__(vehicle_logic, config, logger)
        
        # Configure which longitudinal controller to use
        # Options: 'cacc', 'pi', 'hybrid'
        self.controller_type = getattr(config, 'longitudinal_controller_type', 'cacc')
                                                                                 ^^^^
                                                                    This is the default ────┘

┌─────────────────────────────────────────────────────────────┐
│ STEP 3: Change the default value to what you want          │
└─────────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ OPTION A: Use CACC (Recommended for platoon following)    ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

self.controller_type = getattr(config, 'longitudinal_controller_type', 'cacc')
                                                                        ^^^^^^
                                                                Keep 'cacc' ──┘

✓ Uses CACC controller
✓ Controls both spacing AND velocity
✓ Best for precise formation following

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ OPTION B: Use PI (Simple velocity tracking)               ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

self.controller_type = getattr(config, 'longitudinal_controller_type', 'pi')
                                                                        ^^^^
                                                        Change to 'pi' ──┘

✓ Uses PI controller
✓ Only tracks velocity
✓ Simpler, easier to tune

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ OPTION C: Use Hybrid (Auto-switching)                     ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

self.controller_type = getattr(config, 'longitudinal_controller_type', 'hybrid')
                                                                        ^^^^^^^^
                                                    Change to 'hybrid' ──┘

✓ Uses CACC when leader available
✓ Falls back to PI when not
✓ Best for mixed scenarios

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ That's it! Save and restart your vehicle.                 ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

┌─────────────────────────────────────────────────────────────┐
│ ADVANCED: Custom Parameters (Optional)                      │
└─────────────────────────────────────────────────────────────┘

Want to customize CACC behavior? Find line ~60 in the same file:

    if self.controller_type == 'cacc':
        params = {
            's0': 1.5,              ← Minimum spacing (meters)
            'h': 0.5,               ← Time headway (seconds)
            'K': np.array([[0.2, 0.05]]),  ← [spacing_gain, velocity_gain]
            'acc_to_throttle_gain': 0.5,
            'max_throttle': 0.3
        }

Examples:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Conservative (Safe, larger spacing):
    's0': 2.0,
    'h': 0.7,
    'K': np.array([[0.15, 0.03]]),

Aggressive (Tight, fast response):
    's0': 1.0,
    'h': 0.3,
    'K': np.array([[0.3, 0.08]]),

┌─────────────────────────────────────────────────────────────┐
│ 📊 What Each Parameter Does                                 │
└─────────────────────────────────────────────────────────────┘

s0          Minimum spacing between vehicles
            ↑ Increase → More safe distance
            ↓ Decrease → Tighter formation

h           Time headway (spacing = s0 + h × velocity)
            ↑ Increase → Spacing grows with speed
            ↓ Decrease → Spacing less speed-dependent

K[0]        Spacing error gain
            ↑ Increase → Faster spacing correction (may oscillate)
            ↓ Decrease → Gentler spacing correction

K[1]        Velocity error gain
            ↑ Increase → Better velocity tracking
            ↓ Decrease → Less aggressive velocity matching

acc_to_throttle_gain    Converts acceleration to throttle
            ↑ Increase → More responsive
            ↓ Decrease → Smoother

max_throttle    Maximum throttle output
            ↑ Increase → Can accelerate/brake harder
            ↓ Decrease → Smoother, gentler

┌─────────────────────────────────────────────────────────────┐
│ 🧪 How to Test                                              │
└─────────────────────────────────────────────────────────────┘

1. Change controller type (Step 2 above)
2. Save the file
3. Restart your vehicle script
4. Watch the logs for:
   [FOLLOW] Initialized CACC longitudinal controller
           ^^^^
   This tells you which controller is active

5. Monitor spacing and velocity tracking
6. Adjust parameters if needed

┌─────────────────────────────────────────────────────────────┐
│ 🐛 Troubleshooting                                          │
└─────────────────────────────────────────────────────────────┘

Problem: Vehicle oscillates
Solution: Reduce K gains → K: [[0.1, 0.02]]

Problem: Can't maintain spacing
Solution: Increase K[0] → K: [[0.3, 0.05]]

Problem: Can't match speed
Solution: Increase K[1] → K: [[0.2, 0.08]]

Problem: Too aggressive
Solution: Lower max_throttle → max_throttle: 0.2

Problem: Import error
Solution: Make sure longitudinal_controllers.py is in Controller/

┌─────────────────────────────────────────────────────────────┐
│ 📚 More Information                                         │
└─────────────────────────────────────────────────────────────┘

README_CONTROLLERS.md       - Detailed documentation
ARCHITECTURE.md             - System diagrams
QUICK_SWITCH_GUIDE.py       - More code examples
IMPLEMENTATION_SUMMARY.md   - Complete overview
test_controllers.py         - Test script

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ 🎉 You're all set! Enjoy easy controller switching!       ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
